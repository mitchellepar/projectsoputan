<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.gunungsoputan">

    <uses-permission android:name="android:permission.CAMERA" />


    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.GunungSoputan">
        <activity android:name=".puncak"></activity>
        <activity android:name=".scanbarcodetamu" />
        <activity android:name=".start" />
        <activity android:name=".darurat" />
        <activity android:name=".bukutamu" />
        <activity android:name=".panorama" />
        <activity android:name=".basecamp" />
        <activity android:name=".pinus1" />
        <activity android:name=".jalurpendakian" />
        <activity android:name=".informasigunung" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name="Capture.capture"
            android:screenOrientation="portrait"
            android:stateNotNeeded="true"
            android:theme="@style/zxing_CaptureTheme"/>

    </application>

</manifest>